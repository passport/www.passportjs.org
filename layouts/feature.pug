extends layout

include mixins
include mixins/app
include mixins/social

block content

  section.features
    h1= title
    |!{content}

    ul
      each feature in features
        li
          i(class=feature.image)
          p= feature.title
