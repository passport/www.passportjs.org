extends layout

include mixins
include mixins/app
include mixins/social

block content

  section.features
    h1= title
    |!{content}

    dl
      each feature in features
        div.feature
          dt
            div
              i(class=feature.icon)
            |!{feature.name}
          dd !{feature.description}